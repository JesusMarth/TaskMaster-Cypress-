{
  "buildCommand": "cd taskmaster-app && npm install && npm run build",
  "outputDirectory": "taskmaster-app/build",
  "framework": "create-react-app",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
} 