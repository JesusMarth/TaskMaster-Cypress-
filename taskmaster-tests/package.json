{
  "name": "taskmaster-tests",
  "version": "1.0.0",
  "description": "Pruebas automatizadas E2E con Cypress para la aplicación TaskMaster",
  "main": "index.js",
  "scripts": {
    "cypress:open": "cypress open",
    "cypress:run": "cypress run",
    "cypress:run:chrome": "cypress run --browser chrome",
    "cypress:run:firefox": "cypress run --browser firefox",
    "cypress:run:edge": "cypress run --browser edge",
    "cypress:run:mobile": "cypress run --config viewportWidth=375,viewportHeight=667",
    "cypress:run:tablet": "cypress run --config viewportWidth=768,viewportHeight=1024",
    "cypress:run:desktop": "cypress run --config viewportWidth=1920,viewportHeight=1080",
    "test": "cypress run",
    "test:open": "cypress open",
    "test:ci": "cypress run --record --key your-record-key"
  },
  "keywords": [
    "cypress",
    "e2e",
    "testing",
    "automation",
    "qa",
    "taskmaster"
  ],
  "author": "TaskMaster QA Team",
  "license": "MIT",
  "devDependencies": {
    "cypress": "^14.5.3"
  },
  "engines": {
    "node": ">=14.0.0",
    "npm": ">=6.0.0"
  }
}
